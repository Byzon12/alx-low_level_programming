#include "main.h"
/**
 * mul - multiplication of two interges
 * @a: first parameter
 * @b: second parameter
 * Return: product of a and b
 */
int mul(int a, int b)
{
	int result = 0;

	result = (a * b);
	return (result);
}
